#vaivora_constants.values.time.offset
from datetime import timedelta

pacific2server  = timedelta(hours=3)
server2pacific  = timedelta(hours=-3)
#anchor_spawn    = timedelta(hours=3)
boss_spawn_02h  = timedelta(hours=2)
boss_spawn_04h  = timedelta(hours=4)
boss_spawn_16s  = timedelta(hours=16)